{
  "version": 2,
  "builds": [
    { "src": "main.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "main.py" }
  ],
  "env": {
    "SECRET_KEY": "The secret key is a secret",
    "DATABASE_URL": "sqlite:///users.db"
  }
}
